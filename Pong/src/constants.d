import dsfml.window;

enum SCREEN_WIDTH = 800;
enum SCREEN_HEIGHT = 600;

enum X_SCALE = SCREEN_WIDTH / 1440.0f;
enum Y_SCALE = SCREEN_HEIGHT / 900.0f;

enum PADDLE_VEL = 9f;

enum BALL_MAX = 10f;
enum BALL_MIN = 6f;

enum up1 = Keyboard.Key.Up;
enum down1 = Keyboard.Key.Down;

enum up2 = Keyboard.Key.W;
enum down2 = Keyboard.Key.S;
